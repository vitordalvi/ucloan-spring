spring:
  application:
    name: ucloan

  datasource:
    url: jdbc:postgresql://localhost:5432/ucloan
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true